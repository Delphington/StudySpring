<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">
    <bean id="testBean"
          class="ico.dependency_injection.TestBean">
        <constructor-arg value="Neil"/>
    </bean>

    <!-- Для создания класса     -->
    <bean id="ClassicMusic"
          class="ico.dependency_injection.ClassicMusic">
    </bean>

    <!--Вот это добавили, чтобы в ручную не создавать-->
    <!--
    <bean id="PlayerMusic"
          class="ico.dependency_injection.PlayerMusic">
        <constructor-arg ref="ClassicMusic"/>
    </bean>
    -->
    <!-- Для значения из файла   -->
    <context:property-placeholder location="classpath:musicPlayer.properties"/>
    <!-- Для создания зависимости через Setter   -->
    <bean id="PlayerMusic"
          class="ico.dependency_injection.PlayerMusic">
        <property name="music" ref  = "ClassicMusic"/>
        <property name="time" value="${playerMusic.time}"/>
        <!-- Spring значет про camelCase, и ое певередет   setMusic в music -->
    </bean>


</beans>